{% if baseline.versions %}
  <div class="wdi-browser-compat">
    <div class="wdi-browser-compat__items">
      <doka-baseline
        versionsInfo="{{ baseline.versions }}"
        dateInfo="{{ baseline.date }}"
        statusInfo="{{ baseline.status }}"
      >
      </doka-baseline>
    </div>
  </div>
{% endif %}
