{% if collections.jsArticles | length %}
  {% if page.url === '/all/'%}
    <h3>Статьи</h3>
  {% endif %}
  <ul>
    {% for post in collections.jsArticles %}
      <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
    {% endfor %}
  </ul>
{% endif %}
